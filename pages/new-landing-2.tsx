import type { NextPage } from 'next';
import NewLanding2 from '../components/new-landing-2/NewLanding2';

/**
 * Experimental immersive landing — Virtualverse-style continuous scroll story.
 * Does not replace / , /landing, or /new-landing.
 */
const NewLanding2Page: NextPage = () => {
  return <NewLanding2 />;
};

export default NewLanding2Page;
