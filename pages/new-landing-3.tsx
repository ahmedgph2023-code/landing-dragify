import type { NextPage } from 'next';
import NewLanding3 from '../components/new-landing-3/NewLanding3';

/**
 * Scroll-driven particle morph landing — first two sections only.
 * Does not replace /, /landing, /new-landing, or /new-landing-2.
 */
const NewLanding3Page: NextPage = () => {
  return <NewLanding3 />;
};

export default NewLanding3Page;
